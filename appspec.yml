version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/Flask-App
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: ec2-user
  AfterInstall:
    - location: scripts/setup.sh
      timeout: 300
      runas: ec2-user
