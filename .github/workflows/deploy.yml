name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Checkout your GitHub repo
    - name: Checkout code
      uses: actions/checkout@v3

    # Step 2: Set up Docker Buildx
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    # Step 3: Login to DockerHub
    - name: Log in to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # Step 4: Build and push the Docker image
    - name: Build and push Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/githubactionsdemo:latest .
        docker push ${{ secrets.DOCKER_USERNAME }}/githubactionsdemo:latest

    # Step 5: SSH into EC2 and deploy
    - name: SSH into EC2 and deploy
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          cd /home/ubuntu

          # Clone the repo if not already present
          if [ ! -d "FlaskApp" ]; then
            git clone https://github.com/VTS072Varun/FlaskApp.git
          fi

          cd FlaskApp

          # Pull latest code
          git pull origin main

          # Stop old container
          bash stop_application.sh || true

          # Start new container
          bash start_application.sh
